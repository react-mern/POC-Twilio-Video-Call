// Import the 'styled' function from 'styled-components'
import styled from "styled-components";

// Styled component for displaying error messages
const ErrorStyle = styled.div`
    color:red;
    font-size:14px;
    margin-top:5px;
`;

// Export the styled component for use in other parts of your application
export default ErrorStyle;